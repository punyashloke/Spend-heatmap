
# Executive Heatmap (Streamlit)
Compact, subtle, board-ready heatmaps. Inputs (1/3/5) on the left; output on the right. Excel/CSV downloads included.

## Hugging Face Spaces
- SDK: Streamlit
- App file: `app.py` (or rename below)
- Python: `3.11` via `runtime.txt`

## Files
- `app.py` — main app (you can rename to `streamlit_multicat.py` if you prefer; just update the Space "App file" setting)
- `requirements.txt`
- `runtime.txt`

## Run locally
pip install -r requirements.txt
streamlit run app.py
